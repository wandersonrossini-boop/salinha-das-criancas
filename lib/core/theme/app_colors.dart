import 'package:flutter/material.dart';

class AppColors {
  // Cores Primárias do Design System (Lúdicas e Vibrantes)
  static const Color azulCeleste = Color(0xFF4DACFF);
  static const Color amareloSol = Color(0xFFFFD166);
  static const Color verdePasto = Color(0xFF06D6A0);
  static const Color laranjaCriativo = Color(0xFFFF9F1C);
  static const Color roxoAcolhedor = Color(0xFF9D4EDD);

  // Cores de Superfície e Fundo
  static const Color background = Color(0xFFF8FAFC); // Deluxe Fundo suave
  static const Color surface = Colors.white;
  static const Color textPrimary = Color(0xFF1E293B);
  static const Color textSecondary = Color(0xFF64748B);
  
  // Alertas (Chamada Rápida)
  static const Color presente = verdePasto;
  static const Color ausente = Color(0xFFCBD5E1);
  static const Color alerta = Color(0xFFEF4444);
}
