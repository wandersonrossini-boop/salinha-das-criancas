import 'package:flutter/material.dart';

class DsColors {
  // Paleta Principal do Aplicativo (Identity)
  static const Color primaryBlue = Color(0xFF3B82F6); // Ensino, Confiança
  static const Color primaryYellow = Color(0xFFFBBF24); // Jogos, Alegria
  static const Color primaryGreen = Color(0xFF10B981); // Biblioteca, Crescimento
  static const Color primaryOrange = Color(0xFFF97316); // Pais, Calor, Família
  static const Color primaryPurple = Color(0xFF8B5CF6); // IA, Inovação, Mistério
  static const Color primarySlate = Color(0xFF64748B); // Relatórios, Organização

  // Backgrounds e Superfícies
  static const Color background = Color(0xFFF8FAFC);
  static const Color surfaceWhite = Colors.white;
  static const Color surfaceLightBlue = Color(0xFFEFF6FF);

  // Textos (Alta Legibilidade)
  static const Color textHighEmphasis = Color(0xFF0F172A);
  static const Color textMediumEmphasis = Color(0xFF475569);
  static const Color textDisabled = Color(0xFF94A3B8);

  // Feedback (Acessibilidade)
  static const Color success = Color(0xFF10B981); // primaryGreen
  static const Color error = Color(0xFFEF4444);
  static const Color warning = Color(0xFFF59E0B);
  static const Color info = Color(0xFF3B82F6); // primaryBlue

  // Sombras e Bordas
  static const Color shadowColor = Color(0xFF0F172A);
  static const Color borderColor = Color(0xFFE2E8F0);
}
